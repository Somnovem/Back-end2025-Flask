# 🧠 Технології серверного програмного забезпечення  
## Backend REST API Project Template

### 📋 Опис проєкту  
Цей репозиторій містить базовий шаблон бекенд-застосунку на **Python + Flask**, призначений для навчальних лабораторних робіт із дисципліни *«Технології серверного програмного забезпечення»*.  
Проєкт поступово розширюватиметься в межах наступних лабораторних, охоплюючи створення REST API, роботу з базами даних, контейнеризацію, деплой та тестування.

---

## ⚙️ Стек технологій
- 🐍 **Python 3.11+**
- 🌶️ **Flask** — веб-фреймворк
- 🐳 **Docker**, **Docker Compose** — контейнеризація
- ☁️ **Render.com** — хостинг і деплой
- 🧪 **Insomnia / Postman** — тестування API

---

## 🧩 Структура репозиторію
project_root/
│
├── app/ # Пакет основного застосунку
│ ├── init.py # Ініціалізація Flask-додатку
│ ├── views.py # Основні маршрути (ендпоінти)
│ ├── models.py # (для наступних лабораторних)
│ └── services/ # Бізнес-логіка
│
├── requirements.txt # Залежності Python
├── Dockerfile # Налаштування контейнера
├── docker-compose.yaml # Конфігурація для запуску сервісів
└── README.md # Цей файл 🙂

---

## 🚀 Локальний запуск

### 1. Клонування репозиторію
bash
git clone https://github.com/<your_username>/<your_repo>.git
cd <your_repo>

### 2. Створення віртуального середовища
python -m venv env
source env/bin/activate  # Linux / MacOS
env\Scripts\activate     # Windows

### 3. Встановлення залежностей
pip install -r requirements.txt

### 4. Запуск застосунку
flask run --host 0.0.0.0 -p 8080

### 5. Користування
Відкрити у браузері:
👉 http://localhost:8080/healthcheck

## 🐳 Запуск у Docker

### 1. Збірка образу
docker build -t backend-template:latest .

### 2. Запуск контейнера
docker run -it --rm -e PORT=8080 -p 8080:8080 backend-template:latest

### або за допомогою docker-compose
docker-compose up --build

### Користування
👉 http://localhost:8080/healthcheck

## 🌐 Деплой на Render.com
### Зареєструйтесь на render.com

### Створіть новий Web Service та підключіть GitHub-репозиторій.

### В полі Build Command вкажіть:
docker-compose build
У полі Start Command:
docker-compose up

### Встановіть змінну середовища PORT=8080.

### Після завершення деплою сервіс буде доступний за публічною URL-адресою, наприклад:
👉 https://your-app.onrender.com/healthcheck

## 🔗 REST API

### GET /healthcheck

Опис: Перевірка стану сервісу.
Приклад відповіді:

{
  "status": "ok",
  "date": "2025-10-13T18:00:00Z"
}

Код відповіді: 200 OK

## 🧠 Автор

### 👤 Жмура Артем Анатолійович

### 🏫 КПІ, 2025 рік